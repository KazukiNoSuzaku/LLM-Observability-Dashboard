# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# LLM providers
anthropic>=0.26.0
openai>=1.0.0
mistralai>=1.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Configuration & validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# OpenTelemetry (distributed tracing)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# Arize Phoenix (tracing visualization — optional, requires Python <3.14)
# arize-phoenix>=4.0.0

# Dashboard
streamlit>=1.29.0
plotly>=5.18.0
pandas>=2.1.0
numpy>=1.26.0

# HTTP client
httpx>=0.25.0

# PostgreSQL / Supabase (async + sync drivers — only needed when DATABASE_URL is postgresql+asyncpg://...)
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Form data parsing (FastAPI)
python-multipart>=0.0.6

# Authentication (JWT — optional; falls back to HMAC tokens if not installed)
PyJWT>=2.8.0

# OAuth2 social login (Google + GitHub via Authlib)
Authlib>=1.3.0
itsdangerous>=2.1.0        # required by Starlette SessionMiddleware (CSRF state)

# Guardrails — PII detection & redaction (Microsoft Presidio)
# Install separately — requires spaCy English model:
#   pip install presidio-analyzer presidio-anonymizer
#   python -m spacy download en_core_web_lg
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# Guardrails — structured output validation
# Install separately:
#   pip install guardrails-ai
#   guardrails hub install hub://guardrails/toxic_language   (optional validator)
guardrails-ai>=0.5.0

# Guardrails — NeMo Guardrails dialogue-flow & topic rails (optional)
# Install separately — also requires a LangChain LLM adapter:
#   pip install nemoguardrails langchain-openai     (if using OpenAI for validation)
#   pip install nemoguardrails langchain-anthropic  (if using Anthropic for validation)
# Then set GUARDRAILS_NEMO_ENABLED=true in .env
# nemoguardrails>=0.9.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.23.0
anyio[trio]>=4.0.0
